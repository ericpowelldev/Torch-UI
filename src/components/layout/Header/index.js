//////////////////////// DEPENDENCIES ////////////////////////

import React from "react";

//////////////////////// COMPONENT ////////////////////////

function Header(props) {
  return <header id="header">{props.children}</header>;
}

//////////////////////// EXPORT ////////////////////////

export default Header;
